@import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@400;500;600;700;800;900&family=Inter:wght@300;400;500;600;700&display=swap');

/* Tailwind directives - temporarily commented until Tailwind works
@tailwind base;
@tailwind components;
@tailwind utilities;
*/

/* Base styles */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

:root {
  font-family: 'Inter', system-ui, Avenir, Helvetica, Arial, sans-serif;
  line-height: 1.5;
  font-weight: 400;

  color-scheme: dark;
  color: rgba(255, 255, 255, 0.87);
  background-color: #0B0B0F;

  font-synthesis: none;
  text-rendering: optimizeLegidity;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

body {
  margin: 0;
  display: flex;
  place-items: center;
  min-width: 320px;
  min-height: 100vh;
  background-color: #0B0B0F;
}

#root {
  width: 100%;
  min-height: 100vh;
}

/* Basic utility classes to replace Tailwind */
.min-h-screen { min-height: 100vh; }
.bg-wb-bg { background-color: #0B0B0F; }
.pt-20 { padding-top: 5rem; }
.container { width: 100%; max-width: 1200px; margin: 0 auto; padding: 0 1rem; }
.flex { display: flex; }
.items-center { align-items: center; }
.justify-between { justify-content: space-between; }
.gap-4 { gap: 1rem; }
.text-wb-accent { color: #6BFF3B; }
.text-wb-text { color: #E5E5EA; }
.font-bold { font-weight: 700; }
.text-xl { font-size: 1.25rem; }
.text-2xl { font-size: 1.5rem; }
.text-3xl { font-size: 1.875rem; }
.p-4 { padding: 1rem; }
.px-4 { padding-left: 1rem; padding-right: 1rem; }
.py-2 { padding-top: 0.5rem; padding-bottom: 0.5rem; }
.rounded { border-radius: 0.25rem; }
.bg-wb-accent { background-color: #6BFF3B; }
.text-black { color: #000; }
.hover\:bg-wb-accent\/90:hover { background-color: rgba(107, 255, 59, 0.9); }
.transition-colors { transition-property: color, background-color, border-color; }
.cursor-pointer { cursor: pointer; }
.grid { display: grid; }
.grid-cols-1 { grid-template-columns: repeat(1, minmax(0, 1fr)); }
.grid-cols-2 { grid-template-columns: repeat(2, minmax(0, 1fr)); }
.grid-cols-3 { grid-template-columns: repeat(3, minmax(0, 1fr)); }
@media (min-width: 768px) {
  .md\:grid-cols-2 { grid-template-columns: repeat(2, minmax(0, 1fr)); }
  .md\:grid-cols-3 { grid-template-columns: repeat(3, minmax(0, 1fr)); }
}


body {
  margin: 0;
  min-width: 320px;
  min-height: 100vh;
  background-color: #0B0B0F;
  color: #E6FFE6;
}

@layer base {
  h1, h2, h3, h4, h5, h6 {
    font-family: 'Orbitron', sans-serif;
  }

  h1 {
    @apply text-4xl font-bold md:text-5xl lg:text-6xl;
    line-height: 1.1;
  }

  h2 {
    @apply text-3xl font-bold md:text-4xl;
    line-height: 1.2;
  }

  h3 {
    @apply text-2xl font-bold;
    line-height: 1.3;
  }

  .font-display {
    font-family: 'Orbitron', sans-serif;
  }
}

@layer components {
  .card {
    @apply bg-wb-card p-6 rounded-xl border border-wb-accent/10 hover:border-wb-accent/30 transition-colors duration-200;
  }

  .btn-primary {
    @apply bg-wb-accent text-wb-bg px-6 py-3 rounded-lg font-semibold font-body
           hover:bg-wb-accent/90 transition-colors duration-200
           focus:outline-none focus:ring-2 focus:ring-wb-accent focus:ring-offset-2 focus:ring-offset-wb-bg;
  }

  .btn-secondary {
    @apply bg-wb-card text-wb-ink px-6 py-3 rounded-lg font-semibold font-body
           border border-wb-accent/20 hover:border-wb-accent transition-colors duration-200
           focus:outline-none focus:ring-2 focus:ring-wb-accent focus:ring-offset-2 focus:ring-offset-wb-bg;
  }

  .glow-text {
    @apply text-wb-accent animate-glow;
    text-shadow: 0 0 20px rgba(107, 255, 59, 0.5);
  }

  /* Custom Scrollbar */
  .custom-scrollbar {
    scrollbar-width: thin;
    scrollbar-color: rgba(107, 255, 59, 0.3) rgba(107, 255, 59, 0.1);
  }

  .custom-scrollbar::-webkit-scrollbar {
    width: 8px;
  }

  .custom-scrollbar::-webkit-scrollbar-track {
    background: rgba(107, 255, 59, 0.1);
    border-radius: 4px;
  }

  .custom-scrollbar::-webkit-scrollbar-thumb {
    background: rgba(107, 255, 59, 0.3);
    border-radius: 4px;
  }

  .custom-scrollbar::-webkit-scrollbar-thumb:hover {
    background: rgba(107, 255, 59, 0.5);
  }
}

@keyframes glow-pulse {
  0%, 100% {
    box-shadow: 0 0 20px rgba(0, 255, 136, 0.2);
  }
  50% {
    box-shadow: 0 0 40px rgba(0, 255, 136, 0.4);
  }
}

@keyframes float {
  0%, 100% {
    transform: translateY(0px);
  }
  50% {
    transform: translateY(-10px);
  }
}

.animate-glow-pulse {
  animation: glow-pulse 2s ease-in-out infinite;
}

.animate-float {
  animation: float 4s ease-in-out infinite;
}
